AlmaU Syllabus AI Manager
Интеллектуальная система для централизованного хранения, автоматической проверки и согласования силлабусов университета AlmaU.

Документация для диплома: docs/diploma_report.md.

📌 Проблема
Отсутствие стандарта: Силлабусы хранятся в разрозненных файлах (Word/PDF) на личных компьютерах.

Сложность проверки: Деканам и УМУ трудно вручную вычитывать сотни документов на предмет формальных ошибок (часы, литература, структура).

Нет версионности: Сложно отследить историю изменений и статус документа.

💡 Решение
Веб-платформа, реализующая подход "Source of Truth" (Единый источник правды). Преподаватель загружает готовый файл силлабуса, система автоматически проверяет его структуру с помощью локального ИИ, а затем документ проходит прозрачный процесс согласования.

🚀 Ключевые функции
File-First подход: Загрузка готовых силлабусов (PDF/Word).

AI-Аудит (Local LLM): Автоматическая проверка загруженного файла на наличие обязательных разделов, корректность часов и актуальность литературы.

Workflow согласования: Прозрачная цепочка статусов: Черновик -> AI Проверка -> Декан -> УМУ -> Утвержден.

Версионность: Хранение истории загрузок (ревизий) для каждого силлабуса.

Каталог дисциплин: База данных курсов и привязка силлабусов к академическим периодам.

Ролевая модель: Разграничение прав доступа (Преподаватель, Декан, УМУ, Админ).

👥 Роли и сценарий работы
Преподаватель: Создает карточку силлабуса и загружает PDF-файл.

Система (AI Worker): В фоновом режиме анализирует текст файла и выдает отчет (есть ли ошибки, соблюдена ли структура).

Преподаватель: Если ИИ нашел ошибки — исправляет файл и загружает новую версию. Если всё ок — отправляет на согласование.

Декан: Просматривает файл и отчет ИИ. Утверждает или возвращает на доработку с комментарием.

УМУ: Проводит финальный контроль и утверждает документ.

🏗 Архитектура
Проект построен по микросервисной логике:

Backend: Django 5 (Python) — управление пользователями, базой данных и workflow.

AI Service: Отдельный процесс (Worker) на базе локальной LLM (Qwen/Llama) для анализа текста без передачи данных в интернет.

Frontend: Django Templates + Tailwind CSS + HTMX.

📊 Схема процесса (Mermaid)
Фрагмент кода
flowchart LR
  A[Преподаватель: Загрузка PDF] --> B{AI Проверка}
  B -->|Ошибки| A
  B -->|Ок| C[Согласование: Декан]
  C -->|Доработка| A
  C -->|Утверждено| D[Согласование: УМУ]
  D -->|Финал| E[Архив утвержденных]
🛠 Установка и запуск
1. Подготовка окружения
Bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
2. Настройка БД
Bash
python manage.py migrate
python manage.py createsuperuser
3. Запуск системы (One-Click)
Для запуска веб-сервера и ИИ-воркера используйте скрипт:

Bash
.\start.bat
(Скрипт автоматически запустит Django Server и Worker в разных окнах).

🧠 AI-модуль
Используется локальная квантованная модель (GGUF формат) для обеспечения приватности данных университета. Модель не генерирует текст, а выполняет роль аудитора: классифицирует содержимое и проверяет соответствие критериям AlmaU.

🎓 Шаблон для диплома (Тезисы)
Актуальность: Переход от хаотичного хранения файлов к цифровой экосистеме.

Цель: Автоматизация рутинной проверки силлабусов с помощью ИИ.

Результат: Сокращение времени на проверку документов и создание единого цифрового архива.

Разработано: Алихан Сапар,AlmaU (2026).
